{
  "name": "ciliegio-shop",
  "version": "1.0.0",
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "pdf-lib": "^1.17.1",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "netlify-cli": "^26.1.0",
    "sharp": "^0.35.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:chrome": "playwright test --project=\"Desktop Chrome\"",
    "test:iphone": "playwright test --project=\"iPhone 14\"",
    "test:android": "playwright test --project=\"Pixel 7\""
  }
}
